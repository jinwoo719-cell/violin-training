# 🎻 바이올린 연습 도우미 — MVP

레슨 받는 학생이 집에서 혼자 연습할 때, **지판 어디를 짚어야 하는지** 보여주고
**실제로 어떻게 짚었는지** 악보 위에 겹쳐서 돌려주는 앱.

---

## 지금 되는 것

**화면 ① 연습 가이드**

- 맨 위에 악보 한 줄 — 조표 · 임시표 · 마디선 · **슬러** · **포지션 띠**
- 색이 곧 활 방향 (파랑 ⊓ 다운 / 주황 ∨ 업). 슬러 하나 = 활 한 번
- 노드가 **지판의 실제 mm 자리**로 떨어짐. 숫자는 손가락, 닿으면 너트에서의 거리가 뜸
- **네 줄(G·D·A·E)을 다 그리고 연주하는 줄만 밝게** — 다른 줄은 손가락 자리만 흐리게
- 지금 몇 포지션인지 계속 표시, 손 옮기는 자리에 점선

**화면 ② 결과 리포트**

- 녹음 → 음정 검출 → **악보 · 음정 · 박자 · 궤적을 같은 가로 좌표에 4층**으로
- 음표를 누르면 **그 부분 내 연주**가 다시 들림 (마디별 / 오차 큰 음만 / 전체)
- 재생 중 세로선 하나가 네 층을 함께 지나감
- 숫자를 말로 바꾼 「오늘의 한마디」

**연습 종류** — 장조 / 자연단조 / 화성단조 / 가락단조 × G·D·A·E현,
한 옥타브(1→3포지션) 또는 1포지션만, 데타셰 / 2음 슬러 / 4음 슬러, BPM 40~120

---

## 실행

```bash
pip install -r requirements.txt
streamlit run app.py
```

마이크가 없어도 **[데모로 보기]** 버튼으로 전체 흐름을 볼 수 있습니다.

### Streamlit Cloud 배포

1. 이 폴더를 그대로 GitHub 리포지토리에 올립니다
2. share.streamlit.io → New app → 리포지토리 선택 → Main file `app.py`
3. 끝. **API 키도 secrets도 필요 없습니다** (전부 계산으로 돌아갑니다)

---

## 파일

| 파일 | 하는 일 |
|---|---|
| `app.py` | 스트림릿 화면 두 개와 상태 관리 |
| `music.py` | 음계 · 조표 · 손가락 · 포지션 · 슬러 · 지판 위치 — **모든 음 정보의 출처** |
| `staff.py` | 악보 한 줄 그리기. 가로 좌표 `note_x()`를 여기서만 정합니다 |
| `screens.py` | 화면 ①(가이드) · 화면 ②(리포트) HTML 생성 |
| `analyze.py` | YIN 음정 검출 + 음별 평균/편차/박자 + 데모 연주 합성 |

의존성은 **streamlit과 numpy 둘뿐**입니다. librosa·CREPE·TensorFlow 없이
YIN을 직접 구현했습니다 (약 40줄). 배포가 가볍고 콜드 스타트가 빠릅니다.

---

## 어떻게 돌아가는가

### 1. 주파수 → 지판 위치

```python
mm = L * (1 - f_open / f)        # L = 328mm (4/4 진동현)
```

줄의 진동수는 진동 길이에 반비례합니다. 검산: 한 옥타브 위는 정확히 줄의 절반.
`f_open`만 바꾸면 네 줄에 그대로 적용됩니다.

### 2. 어디서 손을 옮길지

한 음씩 따로 보면 안 됩니다. **음계 전체를 보고** 정합니다.

1포지션으로 끝까지 닿으면 안 옮기고, 안 닿으면
**3포지션의 1번 손가락 자리**(개방현 + 5반음)에서 옮깁니다.
그 자리가 바로 1포지션 3번 손가락이 짚던 자리라서,
**짚어 보고 소리로 확인한 뒤** 올라갈 수 있습니다.

### 3. 소리 → 숫자 (YIN)

파형을 τ만큼 밀어 자기 자신과 겹쳐 봅니다. 한 주기만큼 밀었을 때 가장 잘 맞으므로
그 τ가 주기입니다. 자기상관만 쓰면 옥타브를 자주 틀리므로
YIN의 **누적 평균 정규화**로 낮은 τ에 불이익을 주고,
**포물선 보간**으로 정수 τ 사이를 메웁니다 (안 하면 센트 단위 정밀도가 안 나옵니다).

정확도: 순수음에서 오차 0.3센트 / 편차 0.03센트. 8초 분석에 0.1초.

### 4. 음 나누기

BPM을 아니까 각 음의 시간이 **이미 정해져 있습니다.**
첫 소리가 난 자리만 찾으면 나머지는 계산으로 맞습니다 — 정렬 문제가 사라집니다.

음의 **가운데(18%~88%)만** 씁니다. 앞은 활을 걸며 자리를 잡는 중이고,
뒤는 다음 음으로 넘어가는 중이라 아직/이미 그 음이 아닙니다.
이걸 안 자르면 옆 음이 섞여 **편차가 실제의 대여섯 배**로 나옵니다.

### 5. 평균과 표준편차를 반드시 나눈다

| | 뜻 | 처방 |
|---|---|---|
| **평균** | 손가락 자리가 틀림 | 짚는 위치를 옮겨라 |
| **표준편차** | 활이 흔들림 | 롱톤 연습을 해라 |

둘을 합치면 "20센트 틀렸다"밖에 안 나오고, 무엇을 고쳐야 할지 알 수 없습니다.

### 6. 박자는 첫 음 기준의 상대값

녹음을 언제 시작했는지는 연주와 상관없습니다.
첫 음을 0으로 놓고, 나머지가 그보다 빨랐는지 늦었는지를 봅니다.

---

## 다음에 할 것

- [ ] **튜닝 화면** — 개방현 4개를 켜는 동안 ① 내 악기의 기준 주파수 ② 내 활 흔들림을
      같이 재서, **허용 범위를 사람마다 다르게** 잡기 (지금은 슬라이더로 수동)
- [ ] 활 연습 — 개방현 롱톤의 음량 곡선
- [ ] 두 줄에 걸친 음계 (지금은 한 줄 + 포지션 이동)
- [ ] 리포트 2종 (학생용 한 줄 / 선생님용 상세)
- [ ] 연습 기록 누적, 곡 라이브러리, MusicXML 업로드

---

## 알려진 한계

- **한 줄짜리 음계만** — 줄을 넘나드는 음계는 아직입니다
- 3포지션에서 11·12반음이 둘 다 4번 손가락 (실제로는 뻗어 짚는 자리)
- 박자 측정은 약 ±20ms의 계통 오차가 있습니다 (모든 음에 똑같이 걸리므로
  첫 음 기준 상대값에서는 상쇄됩니다)
- 반주·메트로놈 소리가 섞이면 음정 검출이 흐려집니다 → **이어폰 필수**
